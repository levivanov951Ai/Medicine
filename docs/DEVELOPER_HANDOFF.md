# DEVELOPER HANDOFF — СМЛаб, Design v1

> **Статус: Design v1 заморожен 2026-09-24.** Документ описывает, что и как реализовывать.
>
> ⚠️ **Значений цветов, размеров и теней в этом файле нет.** Единственный канонический источник токенов — [`../design/tokens.css`](../design/tokens.css). Здесь используются только имена токенов. Это сделано намеренно: дублирование значений уже один раз привело к конфликту (см. раздел 2).

---

## 1. Источники истины

Приоритет при любом расхождении:

| Тип информации | Источник истины |
|---|---|
| Продуктовая логика, реальные факты о клинике | [`../PROJECT_CONTEXT.md`](../PROJECT_CONTEXT.md) |
| Визуальный дизайн | Финальный Claude Design artifact |
| Точные design tokens | [`../design/tokens.css`](../design/tokens.css) — извлечены из финального Design System / экранов |
| Структура компонентов, states, responsive, interactions | Этот документ |
| Дополнительный UX-контекст | [`../DESIGN_BRIEF.md`](../DESIGN_BRIEF.md) |

### Где лежит дизайн

| Источник | Ссылка |
|---|---|
| Макет, 35 артбордов | `https://claude.ai/artifact/AVnDzah8JLuRw8Pou2NHRB` — «Смлаб — сайт клиники» |
| Developer Handoff (оригинал в Claude Docs) | `https://claude.ai/artifact/NpaBgug6jbA5iDUNSCEUtR` |

Каждый артборд опубликован как отдельный HTML-файл по пути `project/<Имя>.dc.html` — см. раздел 8.

---

## 2. Примечание о старой таблице цветов

Оригинальный Developer Handoff в Claude Docs содержит таблицу цветов, значения в которой **не совпадают** с тем, что фактически отрисовано в финальном Design v1 — расходятся 16 токенов из 16.

**Та таблица была ориентировочной.** Сам документ помечает её как приблизительную и ссылается на `tokens.py` — файл генератора, который в артефакт не опубликован и недоступен.

**Актуальный source of truth для цветов — извлечённые токены финального Design v1**, собранные в [`../design/tokens.css`](../design/tokens.css). Они взяты из CSS custom properties в `project/Design-System.dc.html` и сверены с `project/Homepage-Desktop.dc.html` — совпадают побайтово.

Старую таблицу при реализации **не использовать**.

Оригинальный документ сохранён как [`./archive/DEVELOPER_HANDOFF_ORIGINAL_DESIGN_V1.pdf`](./archive/DEVELOPER_HANDOFF_ORIGINAL_DESIGN_V1.pdf) — с пояснением в [`./archive/README.md`](./archive/README.md). Он остаётся полезен как описание структуры компонентов и states, но не как источник цветов.

---

## 3. Карта экранов

35 артбордов: 17 экранов × 2 платформы + Design System. Mobile — **самостоятельная вёрстка**, а не сжатый desktop.

| # | Экран | Файлы макета | Назначение |
|---|---|---|---|
| 1 | Design System | `Design-System.dc.html` (1440×8000) | Токены, компоненты, состояния |
| 2 | Header | `Header-Desktop` / `Header-Mobile` | Шапка и навигация |
| 3 | Главная | `Homepage-Desktop` / `Homepage-Mobile` | Вход в воронку, быстрый переход к записи |
| 5 | Услуги и цены | `Catalog-Desktop` / `Catalog-Mobile` | Каталог услуг по категориям |
| 6 | Страница услуги | `Service-Desktop` / `Service-Mobile` | Описание + врачи, оказывающие услугу |
| 7 | Врачи | `Doctors-Desktop` / `Doctors-Mobile` | Каталог врачей по специальностям |
| 8 | Страница врача | `DoctorProfile-Desktop` / `DoctorProfile-Mobile` | Профиль врача и его услуги |
| 9 | Анализы | `Analyses-Desktop` / `Analyses-Mobile` | Каталог, множественный выбор |
| 10 | Страница анализа | `Analysis-Desktop` / `Analysis-Mobile` | Описание, подготовка, стоимость |
| 11 | Выбранные анализы | `SelectedAnalyses-Desktop` / `SelectedAnalyses-Mobile` | Список перед записью |
| 12 | Запись к врачу | `Booking-Doctor-Desktop` / `Booking-Doctor-Mobile` | Полный booking flow |
| 13 | Запись на анализы | `Booking-Labs-Desktop` / `Booking-Labs-Mobile` | Booking без шага «Врач» |
| 14 | Вход | `Cabinet-Login-Desktop` / `Cabinet-Login-Mobile` | Авторизация отдельно от записи |
| 15 | Личный кабинет | `Cabinet-Dashboard-Desktop` / `Cabinet-Dashboard-Mobile` | Список записей |
| 16 | Детали записи | `Cabinet-Details-Desktop` / `Cabinet-Details-Mobile` | Одна запись, отмена |
| 17 | Профиль | `Cabinet-Profile-Desktop` / `Cabinet-Profile-Mobile` | Данные пациента, уведомления |
| 18 | Акции | `Cabinet-Promos-Desktop` / `Cabinet-Promos-Mobile` | Список акций |
| 19 | Контакты | `Cabinet-Contacts-Desktop` / `Cabinet-Contacts-Mobile` | Адрес, телефон, карта |

### Обязательные состояния

| Экран | States |
|---|---|
| Услуги и цены, Врачи | default, empty (ничего не найдено), loading |
| Анализы | default, выбрано ≥ 1 |
| Выбранные анализы | default, empty (все удалены) |
| Booking (оба) | все 5 шагов, ошибка ввода, таймер истёк, подтверждено |
| Вход | empty, ошибка формата, код отправлен, неверный код, loading |
| Личный кабинет | есть записи, пусто |
| Детали записи | предстоящая, завершённая, отменённая |
| Профиль | default |
| Остальные | default |

### Различия desktop / mobile

- **Каталог услуг:** боковая колонка категорий → горизонтальные чипы.
- **Главная:** сетка карточек → горизонтальный скролл-ряд.
- **Анализы:** плавающая панель выбора справа внизу → sticky-панель у нижнего края.
- **Booking stepper:** шаги с подписями в строку → заголовок шага + прогресс-полоски.
- **Отмена записи:** модалка по центру → bottom sheet.
- **Хлебные крошки на странице услуги:** 3 уровня → 2 уровня, намеренное упрощение.

---

## 4. Booking и авторизация

Проверено по всем четырём booking-файлам: сценария «заявка + звонок администратора» нет нигде, все четыре экрана заканчиваются состоянием «Запись подтверждена» / «Вы записаны».

### Запись к врачу

```
Услуга → Врач → Дата и время → Данные / авторизация → Подтверждение
```

**Стартовый шаг зависит от точки входа:**

| Точка входа | Стартовый шаг | Уже отмечено |
|---|---|---|
| Общая кнопка «Записаться» (шапка, главная) | 1. Услуга | — |
| Со страницы услуги | 2. Врач | Услуга |
| Со страницы врача | 1. Услуга | Врач |
| С услуги в профиле врача | 3. Дата и время | Услуга, Врач |

### Запись на анализы

```
Выбранные анализы → Дата и время → Данные / авторизация → Подтверждение
```

Та же модель без шага выбора врача.

### Авторизация

```
Телефон → OTP → авторизован
```

Отдельной регистрации с паролем нет. Экраны `Booking-Doctor`, `Booking-Labs` и `Cabinet-Login` используют одни и те же компоненты поля телефона, ячеек OTP и строки повторной отправки — **в коде это тоже должен быть один общий компонент, а не три копии.**

### ⚠️ Смена номера телефона — FUTURE / PRODUCT ASSUMPTION

На макете `Cabinet-Profile` есть режим смены номера через повторный OTP. **Это не подтверждённая функция MVP.**

На текущем этапе:

- в профиле **отображается** текущий номер пользователя;
- функциональная смена номера **не реализуется**;
- дополнительная auth-логика ради неё **не создаётся**.

Возврат к вопросу — после подключения реальной авторизации. Состояние «режим смены номера» исключено из списка обязательных states экрана «Профиль» (раздел 3).

---

## 5. Компоненты

Финальные версии со всеми состояниями — в артборде `Design-System.dc.html`.

**Базовые:** Header, Footer, Logo, Button (4 варианта × 5 состояний), Input, Search, Badge, Price, Chip, Card-оболочка, Time Slot (4 состояния), Icons.

**Доменные:**

| Компонент | Где | Состояния |
|---|---|---|
| Doctor Card | Главная, Врачи | default |
| Service Row / Analysis Row | Каталоги | default, со скидкой |
| Selected Analysis Row | Выбранные анализы | default (desktop и mobile одинаковы) |
| Calendar | Booking | доступно, нет времени, прошедшее, выбрано |
| Booking Stepper | Booking | done, current, upcoming |
| Reservation Timer | Booking | active, expired |
| OTP | Авторизация, Booking | entering, error, loading, success |
| Phone input | Там же | — |
| Status Badge | Кабинет | предстоящая, завершена, отменена |
| Appointment Card / Row | Кабинет | ближайшая (акцент), обычная |
| Account Subnav | Кабинет | активная вкладка |
| Empty / Loading / Error state | Каталоги, booking | иконка+текст, skeleton, с действием |
| Toggle Switch | Профиль | on, off |
| Promo Card | Акции | default |
| Map Placeholder | Контакты | заглушка |
| Modal / Bottom Sheet | Отмена записи | desktop-модалка, mobile-sheet |
| Sticky CTA | Выбранные анализы, booking | см. раздел 6 |

> Компонент **MOCK badge** присутствует в Design System, но **в пользовательском интерфейсе не используется** — см. раздел 9.

---

## 6. ⚠️ Что на макетах статично, а в коде должно быть живым

Главный источник ошибок при переносе статичного макета в код.

| В макете | В коде должно быть |
|---|---|
| Sticky CTA — статичный блок с пунктирной рамкой и подписью | Настоящий `position: fixed` / `sticky` у нижнего края, поверх контента, **не перекрывающий последнюю карточку списка** |
| Панель выбранных анализов — статичный демо-блок | То же самое |
| Modal / Bottom Sheet — блок, встроенный в поток страницы | Настоящий диалог: затемнение фона, ловушка фокуса, закрытие по Esc и по клику вне |

**Ещё из аудита дизайна:**

- На каталогах услуг и врачей используются **локальные** версии empty/loading-состояний вместо общих. Визуально сведены, но код не объединён — при вёрстке консолидировать в один компонент.
- Футер на всех экранах собирается из общих частей — **не заводить локальных копий разметки**.
- Промежуточные ширины (планшет) **не макетировались**. Адаптация между 390 и 1440 — на разработчике, по тем же принципам.

---

## 7. Ассеты

| Тип | Состояние |
|---|---|
| **Иконки** | Линейные, обводка 1.75px, скруглённые концы, без заливки, размеры 16/20/24px. Набор: search, phone, calendar, clock, pin, flask, stethoscope, heart, user, mail, chevron-left/right/down, check, check-circle, close, alert, menu, trash, plus, file-text, briefcase, scan, brain, drop, pill, shield, dna, layers, spinner. **Рекомендация:** подключить готовый набор в том же стиле (например Lucide), а не рисовать заново |
| **Шрифт** | Inter, начертания 400/500/600/700. Google Fonts или self-host |
| **Логотип** | Векторизован в цветах системы, готов к передаче как SVG |
| **Фото врачей** | В макетах — placeholder-блоки. Нужны реальные фото, соотношение ≈ 3:4 — **UNKNOWN** |
| **Карта на Контактах** | Заглушка. Нужна интеграция карты (Яндекс / 2ГИС), адрес один: Западный обход, 42к2 |

---

## 8. Как достать точную разметку экрана

```
Artifact read
  url:  https://claude.ai/artifact/AVnDzah8JLuRw8Pou2NHRB
  path: project/<Имя экрана>.dc.html
```

Файл сохраняется локально и дальше читается обычными средствами. Имена — в таблице раздела 3.

**Важно:** это статичный сгенерированный HTML для показа макета, а не production-разметка. Брать из него следует размеры, отступы, цвета, иерархию и тексты — но не структуру DOM.

---

## 9. Данные: FACT и MOCK

**FACT** — можно выводить как сведения о клинике: название **Смлаб**, **один филиал**, адрес **Западный обход, 42к2**, розово-голубой фирменный стиль.

**MOCK** — всё остальное на макетах: врачи, цены, анализы, акции, тексты услуг.

### Правило отображения MOCK

**MOCK — это внутренний статус данных для разработки.** В пользовательском интерфейсе **никаких видимых бейджей или надписей «MOCK» быть не должно.**

Разделение обеспечивается на уровне кода и документации:

- mock-данные лежат в **отдельных изолированных файлах**, помеченных как временные, чтобы удаляться одним действием при подключении CRM;
- в документации явно указано, какие данные mock, а какие подтверждены;
- mock-данные **не выдаются за реальные сведения клиники** ни в текстах, ни в метаданных страниц.

**Расхождение, отмеченное в аудите дизайна:** цена «Специалист №2» разъезжалась между экранами (3800 ↔ 2900 ₽), приведена к 2900 ₽. Это исправление внутри демо-данных, а не факт о клинике.

---

## 10. Контраст и semantic tokens

Палитра Design v1 проверена на WCAG. Найдено 5 групп нарушений, **все исправлены 2026-09-24 через слой semantic tokens** — базовая палитра при этом не менялась. Полные результаты: [`./CONTRAST_AUDIT.md`](./CONTRAST_AUDIT.md).

### ⚠️ Правило вёрстки

**В компонентах использовать semantic-токены, а не примитивы палитры.** Примитив напрямую — только там, где подходящего semantic-токена нет.

Это не стилистическое пожелание: пять сочетаний примитивов не проходят WCAG, и semantic-слой существует именно для того, чтобы их нельзя было применить по ошибке.

| Задача | Использовать | Не использовать |
|---|---|---|
| Ссылка на белом фоне | `--color-text-link` | — |
| Ссылка на `surface` / `blue-50` / `blue-100` | `--color-text-link-on-tinted` | ❌ `--blue-600` (4.13–4.48) |
| Placeholder в поле ввода | `--color-text-placeholder` | ❌ `--ink-400` (3.01) |
| Граница input / select / checkbox / time slot | `--color-control-border` | ❌ `--line-200` (1.21) |
| Разделитель, граница карточки | `--color-border-decorative` | — |
| Кольцо фокуса | `--color-focus-ring` | ❌ `--blue-500` (2.73–2.96) |
| Primary CTA | `--color-cta-bg` + `--color-cta-text` | ❌ `--pink-600` с белым текстом (4.11) |
| Декоративная розовая заливка | `--color-decorative-pink` | — |
| Текст статуса | `--color-text-success` / `-warning` / `-error` | ❌ базовые `--success` / `--warning` |
| Иконка статуса | `--color-icon-success` / `-warning` / `-error` | — |

### Два остаточных вопроса

Оба описаны в `CONTRAST_AUDIT.md` и требуют проверки **при вёрстке конкретных экранов**:

1. **Граница поля на подкрашенной секции** — `--color-control-border` даёт 3.01 против белого интерьера поля, но 2.85 против `surface` снаружи. По строгому прочтению WCAG 1.4.11 это не проходит.
2. **Белый текст на заливке `success`** — 4.14 при норме 4.5. Проверить состояние «Запись подтверждена» в `Cabinet-Dashboard` и `Booking-*`: если там белый текст на зелёной заливке, нужна замена.

---

## 11. Продуктовые правила, обязательные в коде

Соблюдаются независимо от того, как выглядят на макете:

| Правило | Источник |
|---|---|
| Никаких «администратор свяжется» / «ожидайте подтверждения» | FACT 2.4 |
| Дата и время — один экран | PD-03 |
| Резерв слота 5 минут с видимым отсчётом | PD-02 |
| Известные данные повторно не запрашиваются | PD-03 |
| Выбор одного анализа не открывает список выбранных | PD-04 |
| Ecommerce-лексика запрещена | PD-04 |
| Срок отмены не захардкожен в текстах | PD-06 |
| Нет оплаты, выезда на дом, семейных профилей, результатов анализов | PD-09…PD-12 |
| MOCK не виден в интерфейсе | PD-20 |
| Смена номера не реализуется | PD-21 |

---

## 12. Журнал

| Дата | Событие |
|---|---|
| 2026-09-24 | Design v1 заморожен, проведён UX/UI-аудит всех 35 артбордов |
| 2026-09-24 | Дизайн перенесён в репозиторий; зафиксировано расхождение токенов |
| 2026-09-24 | Введены правила источников истины. Значения токенов вынесены в единственный канонический файл. Проведён контраст-аудит. MOCK убран из UI. Смена номера переведена в FUTURE |
| 2026-09-24 | Исправления контраста A–E применены через слой semantic tokens. Базовая палитра не изменена. Оригинальный PDF убран в `docs/archive/`. Добавлено правило вёрстки: в компонентах использовать semantic-токены |
